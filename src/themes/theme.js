import { createTheme } from '@mui/material/styles';


const theme = createTheme({
    palette: {
      primary: {
        main: '#ff9800',
        dark: '#b26a00',
      },
      secondary: {
        main: '#ff9100',
        light: '#ffa733',
      },
    },
});

export default theme;