import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
          main: '#555588',
        },
        secondary: {
          main: '#ddffff',
        },
      },
});

export default theme;
